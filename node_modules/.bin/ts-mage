#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/ts-node" ]; then
  "$basedir/ts-node"  "$basedir/../mage/bin/ts-mage" "$@"
  ret=$?
else 
  ts-node  "$basedir/../mage/bin/ts-mage" "$@"
  ret=$?
fi
exit $ret
